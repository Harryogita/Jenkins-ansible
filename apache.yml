---
 - hosts: 192.168.137.129
   remote_user: harry
   tasks:
     
     - name: run date command
       command: df -Th
       register: dateval
     - debug:
          var: dateval

    

     - name: Install docker on ubuntu remote server
       apt: name=docker state=present
